# Copy to ".env" in this folder (same directory as foundry.toml).
# Forge loads .env automatically — no need to export PRIVATE_KEY in the shell.
#
# Default below is Anvil account #0 — LOCAL DEV ONLY, never use on mainnet.
PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
